//Routing
  //Routing in a React app
  >Install a third party package
  >routes.js file to configure the routes
  >For each route,create a component file,
  export the component,import it in routes.js 
  and configure the new route with a path propery
  
  //Routing in a Next.js app
  >file-system based routing mechanism
  >when a file is added to the pages folder in a project,
   it automatically becomes available as a route
  >By mixing ang matching file names with nested folder structure,
  it is a possible to pretty much define the most common routing patterns
  
  //Routing in a Next.js app
  >Route with Pages
  >Nested routes
  >Dynamic routes
  >Catch-all routes
  >Navigate from the UI
  >Programmatically navigate b/w Pages
